/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['composterasur.cl', 'storage.googleapis.com'],
      },
      output: 'export',
}

module.exports = nextConfig
